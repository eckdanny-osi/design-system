import DS from '@cwds/core'
import cn from 'classnames'
import { Card } from '@cwds/components';
console.log(DS);

## Spacing

<Card>
  {
    Object.keys(DS.spacing.spacers)
      .map(label => ({ label, value: DS.spacing.spacers[label]  }))
      .map(({ label, value }, i) => {
        return <div
          key={label}
          className={cn(
            'd-flex justify-content-between align-items-center',
            {
              'mt-2': i !== 0,
            }
          )}
        >
        <div
          style={{
            backgroundColor: 'green',
            height: `${value}${DS.spacing.spacerUnit}`,
            width: `${value}${DS.spacing.spacerUnit}`,
          }}
        >&nbsp;</div>
        <div>{label}</div>
        <div>{value}</div>
        </div>
      })
  }
</Card>
