import React from 'react'
import PropTypes from 'prop-types'
import cn from 'classnames'

const propTypes = {}

const defaultProps = {}

const Legend = props => <legend {...props} />
Legend.propTypes = propTypes
Legend.defaultProps = defaultProps

export default Legend
