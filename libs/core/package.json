{
  "name": "@cwds/core",
  "version": "0.3.1",
  "description": "> TODO: description",
  "author": "Danny Eck <danny.eck@osi.ca.gov>",
  "homepage": "",
  "license": "ISC",
  "main": "dist/core.js",
  "module": "dist/core.module.js",
  "style": "dist/styles.css",
  "scripts": {
    "test": "echo \"No tests in @cwds/core\" && exit 0",
    "prebuild": "rimraf dist",
    "build": "run-p build:css",
    "build:css": "node-sass -r -o dist --source-map true --importer ../../sass-importers.js scss",
    "prebuild:watch": "yarn prebuild",
    "build:watch": "run-p build:watch:*",
    "build:watch:css": "yarn build:css && yarn build:css -w",
    "build:watch:json-sass": "onchange variables.json5 -- touch -c scss/_custom.scss"
  },
  "directories": {
    "lib": "dist",
    "test": "__tests__"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "bootstrap": "^4.1.3"
  },
  "peerDependencies": {
    "bootstrap": "^4.1.3"
  }
}
