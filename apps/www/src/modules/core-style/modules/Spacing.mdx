import DS from "@cwds/core";
import cn from "classnames";
import { Card, CardBody } from "@cwds/components";
const { spacers, themeColors } = DS;

## Spacing

<Card>
  <CardBody>
    <table style={{ width: "100%" }}>
      <thead>
        <tr>
          <th>Spacer</th>
          <th className="text-center">Sass</th>
          <th className="text-center">JS</th>
          <th className="text-center">Size</th>
        </tr>
      </thead>
      <tbody>
        {Object.keys(spacers)
          .map(label => ({ label, value: spacers[label] }))
          .map(({ label, value }, i) => {
            return (
              <tr key={label} style={{ height: "3rem" }}>
                <td>
                  <div
                    style={{
                      backgroundColor: themeColors.accent,
                      height: `${value}`,
                      width: `${value}`
                    }}
                  >
                    &nbsp;
                  </div>
                </td>
                <td className="text-center">
                  <tt>$spacer-{label}</tt>
                </td>
                <td className="text-center">
                  <tt>spacers[{label}]</tt>
                </td>
                <td className="text-center">{value}</td>
              </tr>
            );
          })}
      </tbody>
    </table>
  </CardBody>
</Card>
