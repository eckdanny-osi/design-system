import { Modal, ModalBody, ModalFooter, ModalHeader, Button, Icon, IconButton, Card, CardBody } from '@cwds/components'
import DemoCard from '../../../../components/DemoCard'
import PropTable from '../../../../components/PropTable'
import ModalExample from "./Modal.example.jsx";
import Docgen from '../ReactDogenInfo.js'

## Synopsis

`Modal`s are much like icon popover's but with more functionality rather than just some text.

<DemoCard>
  <ModalExample />
</DemoCard>

```jsx
import { Modal } from '@cwds/components'
```


## Props

<PropTable docgen={Docgen.Modal} />
