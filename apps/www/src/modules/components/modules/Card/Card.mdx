import {
  Card,
  CardHeader,
  CardBody,
  CardFooter,
  Button,
  CardTitle,
  Placeholder,
} from '@cwds/components'
import DemoCard from '../../../../components/DemoCard'
import LoadingCardExample from './LoadingCardExample.jsx'
import MultiScopeCardExample from './MultiScopeCard.example.jsx'
import PropTable from '../../../../components/PropTable'
import Docgen from '../ReactDogenInfo.js'

## Synopsis

Use a `Card` to visually organize related content.

<DemoCard>
  <Card className="mb-0">I am a Card</Card>
</DemoCard>

```jsx
import { Card } from '@cwds/components'
```

## Props

<PropTable docgen={Docgen.Card} />

## Usage Guidelines

@todo

## Examples

@todo

### Card Structure

alskdjfalsdkfj

<Card>I'm in a Card!</Card>

But they can also

<Card>
  <CardHeader>
    <CardTitle>asldkfj</CardTitle>
  </CardHeader>
  <CardBody>CardBody</CardBody>
  <CardFooter>CardFooter</CardFooter>
</Card>

asldkfj

### Loading

asldkfj

<LoadingCardExample />

```jsx
<Card>
  <CardHeader>
    {this.state.isLoading ? (
      <Placeholder>Uknown Person</Placeholder>
    ) : (
      <CardTitle>Fred Jones</CardTitle>
    )}
  </CardHeader>
  <CardBody>
    {this.state.isLoading ? (
      <Placeholder cols={2} rows={3} />
    ) : (
      <FriendsList friends={this.data} />
    )}
  </CardBody>
</Card>
```

asdfasdf

<Card>
  <CardHeader>
    <CardTitle>Loading Card</CardTitle>
  </CardHeader>
  <CardBody>
    <Placeholder cols={2} rows={5} />
  </CardBody>
  <CardFooter>
    <Button color="primary">Go!</Button>
  </CardFooter>
</Card>

asldkfj

### MultiScope Cards (WiP)

asdflkj

<MultiScopeCardExample />

## Props

@todo

## Related

@todo

### Patterns

@todo

### Components

@todo
