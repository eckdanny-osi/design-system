import { Alert } from "@cwds/components";
import { DemoCard, PropTable, ReferenceCard } from "@cwds/docs";
import Docgen from "../ReactDogenInfo.js";

## Synopsis

Use `BreadcrumbItem`s inside the `Breadcrumb` component.

See the [`Breadcrumb` article](/components/breadcrumb) for usage.

## Props

<PropTable docgen={Docgen["BreadcrumbItem"]} />
