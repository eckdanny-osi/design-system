import {
  Modal,
  ModalBody,
  ModalFooter,
  ModalHeader,
  Button,
  Icon,
  IconButton,
  Card,
  CardBody
} from "@cwds/components";
import { CodeBlock, DemoCard, PropTable } from "@cwds/docs";
import {
  BasicModalExample,
  BasicModalExampleSrc,
  SizeModalExample,
  ScrollingModalExample,
  AlertModalExample,
  AlertModalExampleSrc
} from "./examples";

import Docgen from "../ReactDogenInfo.js";

## Synopsis

Use a `Modal` when you want users to focus on a specific task within the context of the original page.

<DemoCard>
  <BasicModalExample />
</DemoCard>

```jsx
import { Modal } from "@cwds/components";
```

## Props

<PropTable docgen={Docgen.Modal} />

## Examples

### Basic

<BasicModalExample />

<CodeBlock>{BasicModalExampleSrc}</CodeBlock>

### Size

<div>
  {[
    { value: "sm", label: "small" },
    { value: undefined, label: "" },
    { value: "lg", label: "large" },
    { value: "xl", label: "extra large" }
  ].map(({ value: size, label }, i) => (
    <SizeModalExample key={i} size={size} label={label} className="pb-2" />
  ))}
</div>

### Scrolling

For long scrolling, default class name is `modal-dialog-scrollable`

<ScrollingModalExample />

### Alerts

<AlertModalExample />

<CodeBlock>{AlertModalExampleSrc}</CodeBlock>

<!--<ModalMoreExample />-->
